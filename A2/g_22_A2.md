```mermaid
%%{init: {"themeVariables": { "fontSize": "12px", "nodeSpacing": "20", "rankSpacing": "25"}}}%%
flowchart LR
    A[Update submodules for each group] --> B[Read new README file and see what part of the report is being]
    B --> C{Need latest from groups?}
    C -- yes --> D[Update submodules<br>`git submodule update --remote --merge`]
    C -- no --> E[Use current pinned SHAs]
    D --> F[Commit updated pointers<br>`git add . && git commit -m Update_submodules`]
    F --> G[Push<br>`git push`]
    E --> H[Place IFCs in models/]
    H --> I[Run analysis<br>`python main.py` or import submodule scripts]
    I --> J{Results OK?}
    J -- yes --> K[Commit results/configs<br>`git add && commit && push`]
    J -- no --> L[Adjust inputs/scripts] --> I
